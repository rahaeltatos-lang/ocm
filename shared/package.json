{
  "name": "ocm-shared",
  "version": "0.1.0",
  "description": "Shared types and utilities for OCM",
  "main": "types.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
